#!/usr/bin/env bash

BASH_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)

# Includes
# shellcheck source=/dev/null
source "${BASH_DIR}/_variables.sh"
# shellcheck source=/dev/null
source "${BASH_DIR}/helpers.sh"
